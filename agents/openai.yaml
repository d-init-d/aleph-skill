interface:
  display_name: "Aleph Skill"
  short_description: "Adaptive causal timelines across past and future"
  default_prompt: "Use $aleph-skill and its locked bundled D Research gateway to build an auditable causal timeline with separate research and sealed roleplay tracks."
policy:
  allow_implicit_invocation: true
