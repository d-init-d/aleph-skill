interface:
  display_name: "Aleph Skill"
  short_description: "Adaptive causal timelines across past and future"
  default_prompt: "Use $aleph-skill to build an auditable evidence-backed or assumption-only causal timeline with explicit uncertainty and sealed actor roleplay."
policy:
  allow_implicit_invocation: true
